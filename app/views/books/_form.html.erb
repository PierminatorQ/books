<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :title, "Titulo del Libro"  %>
    <%= form.text_field :title, class:"form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :author, "Autor"%>
    <%= form.text_field :author, class:"form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :state, "Estado" %>
    <%= form.select :state, Book.statuses.keys, class:"form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :lend_date, "Fecha de Prestamo" %>
    <%= form.date_select :lend_date, class:"form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :return_date, "Fecha de devoluciòn" %>
    <%= form.date_select :return_date, class:"form-control" %>
  </div>

  <div class="actions">
    <%= form.submit "Crear Libro", class: "btn btn-info" %>
  </div>
<% end %>
